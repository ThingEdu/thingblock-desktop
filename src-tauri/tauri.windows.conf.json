{
  "$schema": "../node_modules/@tauri-apps/cli/config.schema.json",
  "build": {
    "beforeDevCommand": "npm run stage:sidecar:win && npm run dev:editor",
    "beforeBuildCommand": "npm run stage:sidecar:win && npm run build:editor && npm run stage:frontend:win"
  },
  "bundle": {
    "targets": [
      "nsis"
    ]
  }
}
